<%= form_for(@fav_line_item) do |f| %>
  <% if @fav_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fav_line_item.errors.count, "error") %> prohibited this fav_line_item from being saved:</h2>

      <ul>
      <% @fav_line_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :fav_id %><br />
    <%= f.number_field :fav_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
